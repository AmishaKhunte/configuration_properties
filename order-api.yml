server:
  port: 8083

spring:
  application:
    name: user-api

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/mymart_user_db
    username: root
    password: rootuser

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  boot:
    admin:
      client:
        url: http://localhost:1111

  mail:
    host: smtp.gmail.com
    port: 587
    username: amishakhunte60@gmail.com
    password: "yvuw dwzd lbht dsty"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

management:
  endpoints:
    web:
      exposure:
        include: "*"

eureka:
  instance:
    prefer-ip-address: true

razorpay:
  key:
    id: rzp_test_Sts4ErOLa4LYpS
    secret: dTmJ7gqydC4LCIXmbkrrqVJ3

  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    register-with-eureka: true
    fetch-registry: true
